#include <stdio.h>

int main() {
    char ch;

    // Taking a single character input
    scanf("%c", &ch);

    // Printing the ASCII value of the character
    printf("ASCII Value: %d", ch);

    return 0;
}
